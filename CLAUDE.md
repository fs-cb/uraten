# CLAUDE.md — URATEN サイト実装の前提と制約

このリポジトリは URATEN（福岡・天神のサブカルクリエイター向け24時間Webラジオ）の公開サイト。
本ファイルは実装時に必ず守る決定事項の正本。ここに書かれた制約と衝突する実装・提案はしない。

**設計判断（アーキテクチャの変更・スキーマの変更・新しい仕組みの追加）はこのファイルの範囲外であり、実装者が独断で行わない。提案として運営者に返すこと。**

ただし、運営者から**個別の実装仕様書が渡された場合は、その仕様書が本ファイルに優先する**。仕様書は運営者が本ファイルを踏まえて判断した結果であるため。その際、本ファイルとの衝突点を作業後に報告し、本ファイルの更新につなげること。

最終更新：2026年9月25日

---

## 1. アーキテクチャの不変条件（違反禁止）

1. **Sheets は審査台、JSON は公開データ。両者を直結させない。**
   - Google Sheets には未審査の申請・運営メモが含まれる。ブラウザ（公開サイト）から Sheets を直接読む実装（「ウェブに公開」CSV / gviz / API直叩き等）は、どんなに効率的でも**絶対に採用しない**。
   - 公開サイトが読んでよいデータは、審査・承認を経て公開されたものに限る。具体的には次の2つのみ。
     - リポジトリ内の静的 JSON（`data/*.json`）
     - 運営パイプライン（`uraten-ops`）が R2 `uraten-media` に置いた公開 JSON（`https://media.ura-ten.jp/` 配下。現状は `calendar/calendar.json` のみ）
   - 上記以外の読み込み先（外部 API 等）を足す場合は、実装前に運営者へ確認を返す。

2. **状態を持たない。サイトは静的が原則。**
   - 予約状況・残数・会員・セッションなどの状態管理を導入しない。公開サイトは静的サイト（Cloudflare Pages・ビルドコマンドなし）。
   - **例外として、BGM原本の配信にのみ Cloudflare Workers ＋ R2 を使用している**（下記 1-5）。これは「非公開バケットから条件付きで配信する」という、静的サイトでは原理的に実現できない要件に対する導入であり、既に稼働中。
   - **これ以外に DB / KV / D1 / 新規 Workers / サーバーレス関数を足す提案をしない。** 「便利になるから」は理由にならない。導入は運用が限界に来たと運営者が判断した時のみ。
   - Worker 側でも状態を持たない。セッション・ダウンロード履歴・利用者の識別を実装しない。

3. **公開の判断は必ず人が行う。**
   - 公開してよいかの判断は、運営者が審査台で「承認」を付ける行為で行う。承認が公開の意思表示であり、承認と公開を分離しない。
   - 自動化してよいのは「承認済みデータの変換と配置」のみ。**未承認のデータが公開経路に乗りうる自動処理は実装しない。**
   - イベントカレンダーは、`uraten-ops` の変換スクリプトが cron（毎時）で承認済み行だけを JSON 化して R2 に置く（5-1）。このリポジトリ側に変換・公開の処理は置かない。
   - `data/*.json`（BGM 等）は従来どおり、手で更新して git diff で目視確認してから push する。

4. **git push が公開。** push 後は Cloudflare Pages が自動デプロイする。**デプロイ設定（フレームワークなし・ビルドなし・出力ルート）は変更しない。**
   - この制約は Worker 導入時にも維持された。Git 接続の Pages には公開対象から除外する仕組みがないため（`.cfignore` は Wrangler 直接アップロード専用、`_redirects` は 404 を返せない）、**Worker のコードはリポジトリ外に切り出す**ことで解決している。同種の問題が起きた場合も、ビルドコマンドの導入ではなくリポジトリ分割で解く。

5. **リポジトリは2つ。デプロイ経路も2つ。**

   | | サイト | Worker |
   | --- | --- | --- |
   | リポジトリ | 本リポジトリ | `uraten-bgm-dl-worker`（private） |
   | デプロイ | git push → Pages 自動 | `wrangler deploy`（手動） |
   | 用途 | 公開サイト全体 | BGM原本の配信のみ |

   - **Worker のコードを本リポジトリに置かない。** 置くと Pages が `/worker/index.js` として公開してしまう。
   - Worker のエンドポイント URL を変更した場合、`bgm-dl.html` の `workerBase` も同時に更新する（相互依存）。

## 2. セキュリティ（違反禁止）

- **service account の鍵 JSON をリポジトリに絶対に入れない。** 鍵はリポジトリ外に置き、パスは環境変数か設定ファイル（gitignore 対象）で渡す。`.gitignore` に鍵ファイルのパターンを必ず入れる。
- **合言葉・シークレットをリポジトリに入れない。** BGMダウンロードの合言葉は Cloudflare の Secret に保管する（`wrangler secret put`）。`wrangler.toml`・HTML・JavaScript のいずれにも平文で書かない。ダッシュボードで値を変更するだけで反映される状態を維持する（再デプロイを要する実装にしない）。
- **Worker 側リポジトリの `.gitignore` に `node_modules/` `.wrangler/` `.dev.vars` を必ず含める。**
- **公開 JSON（`data/*.json` および R2 の `calendar/calendar.json`）に運営用情報を出さない。** 審査メモ・主催者の氏名や連絡先・誓約チェックの生データ・Tally の内部 ID（Submission ID・受付ID を含む）等は Sheets に留める。公開 JSON に載せてよいのは公開表示に必要な項目のみ。
- **公開 JSON に非公開リソースの所在を書かない。** BGM原本のパスを `bgm.json` に持たせないのはこのため。原本のキーは ID から機械的に導く。
- **R2 の非公開バケット（`uraten-bgm-master`）を公開しない。** カスタムドメインも `r2.dev` も接続しない。Worker のバインディング経由でのみ読む。
- **利用者入力を R2 のキーとしてそのまま使わない。** ID は形式（`URT-B-(CM|TK)-\d{3}`）とカタログ掲載の両方で検証してから使う。
- 公開サイトに申請者が直接書き込める経路を作らない。R2 / リポジトリへの書き込みは運営（変換スクリプト・手動アップロード）のみ。
- 申請由来の文字列（イベント名・会場名等）は DOM へ `textContent` で挿入する。`innerHTML` への直接展開をしない。
- ブラウザに `localStorage` を使わない。`sessionStorage` は合言葉の保持に限り可（タブを閉じたら消える範囲）。

## 3. リポジトリ構成

```
index.html          … トップページ（ラジオプレーヤー・イベントカレンダー直近5件）
calendar.html       … イベントカレンダー全件表示（app.js は読み込まない）
menu.html           … サービスメニュー
bgm.html            … BGM試聴ページ（公開）
bgm-dl.html         … BGM原本ダウンロードページ（合言葉ゲート・noindex）
terms.html          … 利用規約
tokushoho.html      … 特定商取引法に基づく表記
refund.html         … 返金・キャンセルポリシー
privacy.html        … プライバシーポリシー

css/style.css       … トップページ系スタイル
css/menu.css        … メニュー系スタイル（BGMページも読み込む）
css/legal.css       … 法務ページ系スタイル
css/bgm.css         … BGMページ系スタイル

js/app.js           … トップページ（プレーヤー等。トップ固有の DOM が前提）
js/calendar.js      … カレンダーの取得・日付判定・描画（トップと calendar.html で共用）
js/menu.js          … メニューページ
js/bgm.js           … BGM一覧の描画・絞り込み・試聴（試聴/DL 両ページで共用）
js/bgm-dl.js        … 合言葉ゲートとダウンロードボタン（bgm-dl.html 専用）

data/bgm.json       … BGMカタログ（48件）
data/bgm-tags.json  … BGMのタグ語彙マスタ

images/             … 掲載イラストのサンプル（sample-01〜08.webp）

.gitattributes
README.md           … デプロイ手順・スキーマ定義
CLAUDE.md           … 本ファイル
```

- **CSS・JS はページ系統ごとに1ファイル。** この粒度を保つ。1ページ分をさらに複数に割る、JS をモジュール群に分解する、ビルドツール・フレームワークを導入する、といった細分化は行わない。
- **同じ処理を2ファイルにコピーしない。** 試聴ページとDLページの一覧描画が `js/bgm.js` を共用しているのがその例。共通化のために既存ファイルを動かす場合は、両方のページの動作確認を行う。
- **`css/menu.css` は BGM ページからも読み込まれている。** 変更時はメニュー・BGM試聴・BGMダウンロードの3ページを確認する。
- 相対パス前提（`./css/` `./js/` `./data/` `./images/`）。階層構造を変えない。

## 4. データスキーマ（連携の契約・勝手に変えない）

### 4-1. イベントカレンダー `https://media.ura-ten.jp/calendar/calendar.json`

`uraten-ops` の `calendar_publish.py` が生成し、`js/calendar.js` が読む。このリポジトリは読み手であり、生成側のコードは持たない。スキーマ変更は両者の合意で行う。

```json
{
  "generated_at": "2026-09-25T17:10:00+09:00",
  "events": [
    {
      "name":   "イベント名",
      "start":  "YYYY-MM-DD（必須・JST）",
      "end":    "YYYY-MM-DD または null（複数日開催のみ）",
      "venue":  "会場/形式",
      "url":    "資料URL（http/https のみ）",
      "notice": false
    }
  ]
}
```

- `events` は開始日順。終了日（なければ開始日）が6ヶ月前の月初以降のものが入る。
- **公開データなので、表示に必要な項目しか持たない。** イベントID・受付ID・主催者情報等は含まれない。項目を足す場合は設計判断として運営者に返す。
- `notice` は中止・延期等の「お知らせあり」。文言は持たない（詳細はリンク先で確認してもらう）。
- 開催時間・主催者名は意図的に持たない。
- 未知のフィールドが増えても無視して動くこと。

**フロント側仕様：**

- 日付は `YYYY-MM-DD` 文字列のまま比較し、`Date` に通さない。「今日」は Asia/Tokyo で判定する。
- 終了判定は `end`（なければ `start`）が今日より前。「開催中」の判定・表示は行わない（リアルタイムの進行を追う用途ではないため）。
- トップ：終了していないものを開始日順に最大5件。超過分は「ほか◯件のイベント」。
- `calendar.html`：月見出し＋開始日順。これからのイベントの下に、終了したイベントを**新しい順**で折りたたむ。
- **同じ開始日のイベントは読み込みごとにシャッフルする。** 掲載料を受けているイベント間で露出に偏りを作らないための仕様（10章・Pay to Win の禁止）。外さない。
- 「お知らせあり」バッジはトップ・`calendar.html` の**両方**に出す（中止されたイベントへの誘導を防ぐため）。
- 行全体を `url` へのリンクにする（`target="_blank" rel="noopener noreferrer"`）。http/https 以外はリンクにしない。
- 読み込み中／0件／エラーの3状態を表示する。

### 4-2. `data/bgm.json`

`js/bgm.js`・`js/bgm-dl.js`・Worker の3者が依存する。**Worker はカタログを HTTP で取得して ID の実在確認に使う**ため、構造変更は Worker 側にも影響する。

```json
{
  "version": "更新日",
  "preview_base": "https://media.ura-ten.jp/",
  "items": [
    {
      "id":       "URT-B-CM-001",
      "title":    "表示名",
      "category": "cm | talk",
      "duration": 29,
      "tags":     ["雰囲気1〜2", "テンポ1"],
      "preview":  "bgm/preview/URT-B-CM-001.mp3",
      "preview_duration": 75
    }
  ]
}
```

- `id` は不変。ファイル名・R2のキー・mp3 の内部メタデータと一致する。**採番済みの ID を変更しない。差し替えや取り下げが出ても欠番のまま残す。**
- `title` は可変。表示にのみ使われ、ファイルの所在には影響しない。
- `duration` は**原本の尺**（秒・整数）。試聴版の尺ではない。
- `preview_duration` は**トーク用のみ**存在（値は75）。試聴が抜粋であることを示す。CM用には存在しない。
- 音源URLは `preview_base` ＋ `preview` で組み立てる。**ベースURLをコードにハードコードしない。**
- **原本のパスは持たせない**（公開 JSON に非公開リソースの所在を書かないため）。原本のキーは `{id}.mp3`。

### 4-3. `data/bgm-tags.json`

```json
{
  "version": "更新日",
  "groups": [
    { "key": "mood",  "label": "雰囲気", "tags": ["明るい","落ち着き","おしゃれ","シリアス","かわいい","かっこいい","あたたかい"] },
    { "key": "tempo", "label": "テンポ", "tags": ["速い","ふつう","ゆっくり"] }
  ]
}
```

- **タグの表示順はこのファイルの配列順に従う。** `bgm.json` から動的に集めて並べない（表記ゆれと順序崩れを防ぐため、語彙をマスタで固定している）。
- 絞り込みロジック：**同一グループ内は OR、グループをまたぐと AND。** 雰囲気タグは1件につき1〜2個しか付かないため、同グループを AND にするとほぼ 0 件になる。
- **選択中のカテゴリで 0 件になるタグはボタンを表示しない。**
- タグ語彙は運用しながら増減しうる。このファイルの差し替えだけで反映される構造を保つ。

## 5. データパイプライン

種別によって経路が違う。混同しない。

### 5-1. イベントカレンダー（Sheets → uraten-ops → R2）

```
申請者 → Tally フォーム（1〜2件／3〜6件／7〜10件の3種）
  → Google Sheets「URT-Calendar」（タブ 12 / 36 / 710。Tally 連携で自動追記）
  → calendar_expand.py（VPS・cron 毎時0分）: イベント単位に展開して review タブへ追記
  → 運営が review タブで審査し、ステータスを「承認」にする
  → calendar_publish.py（VPS・cron 毎時10分）: 承認済み行のみ JSON 化 → R2 calendar/calendar.json
  → サイト（js/calendar.js）が fetch して表示
```

- パイプラインのコード・設定・運用手順は **`uraten-ops` リポジトリの管轄**。このリポジトリには置かない。
- 承認はイベント単位（review タブの1行＝1イベント）。
- R2 側で `https://ura-ten.jp` からの GET を CORS 許可している。サイトのオリジンを増やす場合は R2 の CORS 設定も更新が要る。
- 公開 JSON はキャッシュ5分。権利申し立て等の即時取り下げ手順は `uraten-ops` 側の手順書に置く。

### 5-2. BGM（Sheets を経由しない）

```
Suno で生成 → ローカルで ffmpeg 一括処理（正規化・メタデータ書換・原本/試聴版の書き出し）
  → 原本: R2 uraten-bgm-master（非公開・手動アップロード）
  → 試聴版: R2 uraten-media（公開・media.ura-ten.jp）
  → data/bgm.json を手で更新 → git push
```

- BGM は申請由来のデータではないため、審査台を経由しない。
- **BGM を追加してもサイト側・Worker 側のコードは変更不要。** R2 にファイルを置き、`bgm.json` に追記するだけで済む構造を維持する。

## 6. ラジオプレーヤー実装の必須要件

プレーヤーは本サービスで最も故障しやすく、かつ事業の最重要仮説（ながら聴取・回遊）の生命線。
**素の `<audio>` タグだけの実装は不可。**

1. **Media Session API を実装する。** ロック画面・コントロールセンターに再生 UI とメタデータを出す。
2. **瞬断からの自動再接続を実装する。** 再生意図がある間だけ、指数バックオフ（2s→最大15s）＋キャッシュバスターで再接続。`playing` 復帰でリセット。
3. **音のエンジンは車輪の再発明をしない。** AzuraCast 公開プレーヤーの実装を土台・参考にし、自作するのは UI の殻。

### 6-1. メタ表示は2ソース（番組枠とフィラーで取得元が違う）

表示のタイトル/サムネは、性質の違う2ソースから来る。混同禁止。

- **番組枠**：Sheets 由来の編成 JSON ＋ クライアントの時刻マッチングで表示。焼き込み番組は Now Playing API では中身が1エントリに潰れて使えないため、API を表示源にしない。
- **フィラー区間**：Now Playing API（`api/nowplaying/uraten`）を **20秒ポーリング**して表示。独立トラックの BGM は API がタイトルを返す。ICY メタは使わない。
- **WebSocket / SSE は現状不採用。** ポーリングで実用上困らない（焼き込み中はタイトルが動かない）。即時反映が実際に要るシグナルが出てから差し替える。
- **表示ガード**：`title`/`artist` が空、または `.mp3` 等 内部ファイル名っぽい値は表示しない。タイトルは「URATEN」フォールバック、アートは出さない。

### 6-2. 表示ソースの優先順位と境界の競合防止【JSON連携時に実装】

番組枠（JSON＋時刻）とフィラー（APIポーリング）は別々の時計で動くため、境界（例 20:00:00）で競合する。書き込む権利は**その瞬間のモード判定に合致したソースだけ**が持つ。

1. 「今 番組枠か / フィラー区間か」を JSON＋現在時刻で高頻度（毎秒程度）に判定する。ローカル計算のみ・負荷ゼロ。ポーリングとは切り離して回す。
2. **番組枠と判定される間は、API のレスポンスを表示に反映しない**（受信しても破棄）。番組表示は JSON が正。
3. フィラー区間の間のみ API 結果を採用する。
4. **非同期の追い越しを殺す。** fetch は投げた時と返る時でモードが変わりうる。**ポーリング結果を使う直前にモード判定をやり直し、番組枠なら破棄する。**「投げた時のモード」でなく「使う瞬間のモード」で採否を決める。

### 6-3. 対象外・割り切り

- **端末時計のズレ**：時刻マッチングはブラウザのローカル時計依存。境界が端末ごとに数秒ずれるが実害なし。NTP 同期等は持ち込まない。
- **短尺フィラー（5〜10秒）の取りこぼし**：20秒ポーリングでは拾えないことがあるが、表示は切り替えなくてよい。独立トラックで表示させたい BGM は最低尺（例 30秒以上）で編成する側で対処し、ポーリング高速化/WS化はしない。

## 7. BGMダウンロードの仕組み

### 7-1. 構成

```
制作者 → bgm-dl.html（Pages・静的）
  → 合言葉を入力 → Worker /verify で照合
  → 一覧表示（js/bgm.js を共用。差分はDLボタンのみ）
  → DLボタン → Worker /download
      → 合言葉を照合 → ID を検証 → R2 uraten-bgm-master から取得 → 返す
```

### 7-2. 崩してはいけない前提

- **原本バケットは非公開のまま。** 「実装が簡単になるから公開する」は却下。
- **照合はダウンロードのたびに行う。** 「一度通ったら以後フリーパス」にしない。ページの入口は導線にすぎず、実際の関門は各ダウンロードにある。
- **合言葉はクライアントで判定しない。** 可否は必ず Worker が返す。
- **合言葉は全員共通・無期限。** 個別発行や有効期限を設けない（発行済みの管理＝状態を持つことになるため）。
- **照合ルール**：前後の空白を除去して比較。大文字小文字は区別する。それ以外の正規化（全角半角変換など）は行わない。
- **合言葉の入力欄には `autocapitalize="off"` を付ける。** iOS Safari が先頭を自動で大文字化するため。
- `bgm-dl.html` はサイト内のどこからもリンクしない。URL は受付メールで案内する。`noindex,nofollow` を維持する。

### 7-3. 割り切り

- **試聴版はダウンロードできる。** ブラウザで再生できるファイルは必ず取得可能であり、技術的に防げない。防御は素材側で行っている（96kbps・トーク用は75秒の抜粋）。落とされても制作素材としては使えない。
- **未払いでの取得を完全には防げない。** 資格ゲートの目的は盗難防止ではなく、無防備な公開配布に比べて母数を絞ることにある。合言葉の強度を上げるより、利用者が入力しやすいことを優先している。

## 8. 現在の状態（2026年9月時点）

**完了：**

- 独自ドメイン `ura-ten.jp` で公開中（Cloudflare Pages・Git 接続・自動デプロイ稼働）
- DNS は Cloudflare 一元管理。配信は `radio.ura-ten.jp`（プロキシOFF・Let's Encrypt 自動更新）、メディアは `media.ura-ten.jp`（R2 カスタムドメイン）
- VPS 契約・AzuraCast 構築・稼働中（ゲート C-1 クリア）
- ラジオプレーヤー：再生/停止・バックグラウンド再生・ロック画面・瞬断自動再接続を実機確認済み
- 法務ページ公開済み（利用規約・特商法・返金ポリシー・プライバシーポリシー）※専門家確認は未実施
- メニューページ公開済み
- **BGM試聴ページ（`bgm.html`）公開済み。** BGM 48件（CM用21・トーク用27）を R2 から配信
- **BGMダウンロードページ（`bgm-dl.html`）＋ Worker 稼働中**
- R2 バケット3つ運用中：`uraten-media`（公開）・`uraten-bgm-master`（非公開）・`uraten-backups`
- Tally フォーム作成済み：放送枠・歌い手枠・CM枠・イラスト（ギャラリー/スライドショー）・イベントカレンダー（3種）
- イベントカレンダー：Sheets→R2 のパイプライン稼働（5-1）。トップ直近5件と `calendar.html` を実装済み

**未着手・未確定：**

- **レギュラー枠のフォーム未作成**（メニューのリンクが `#` のまま）
- プレーヤーのメタ表示（タイトル/サムネ）：JSON連携と同時に実装（6-1・6-2）。現状はポーリングのみで暫定
- ロック画面の再生/停止コントロールの操作確認（iOS Safari 要実機）
- 決済：イベントカレンダーのフォームに Tally の決済ブロック（Stripe）を追加予定。サイト側実装なし

## 9. 実装時の作法

- 変更は小さく刻んでコミットする。コミットメッセージは日本語でよい。
- 見た目の変更（デザイン・文言・レイアウト）は自由に実装してよい。ただし 2章のセキュリティ、3章の構成、4章のスキーマ、7章の前提は影響範囲に含めない。
- **本ファイルと衝突する指示・アイデアが出た場合、実装せず「CLAUDE.md の◯章と衝突する」と運営者に確認を返す。** ただし冒頭のとおり、個別の実装仕様書が渡された場合はそれが優先する。その場合も衝突点を作業後に報告する。
- 決定が変わったら本ファイルを更新する（更新も運営者の確認を経る）。
- **既存ページを壊さない。** 共通化・リファクタリングのために既存ファイルを動かす場合、影響を受ける全ページの動作確認を行う。

## 10. 背景（判断に迷ったときの参照）

- 事業の芯：「お金で枠を買って自分を押し出すのではなく、互いを紹介し合うことで全体が浮上する」。Pay to Win になる機能（金で露出・順位が買える仕組み）は作らない。
- 設計原則：
  - 一人で保守できる範囲に収める
  - 静的で済むうちは静的で
  - 必要が実証される前にインフラを作らない
  - **状態は敵。** 残数・残枠・セッションなど浮遊する値を持たない
  - **コストを収益に比例させない。** 利用が増えるほど手間が増える構造を作らない
  - **機能追加は導入シグナルが出てから。** 「あると便利」では作らない
- 詳細な事業設計・権利設計・サービスメニューは Notion 管理の事業ドキュメントが正本（本リポジトリには置かない）。実装判断に必要な結論は本ファイルに抽出済み。
