# CLAUDE.md — URATEN サイト実装の前提と制約

このリポジトリは URATEN（福岡・天神のサブカルクリエイター向け24時間Webラジオ）の公開サイト。
本ファイルは実装時に必ず守る決定事項の正本。ここに書かれた制約と衝突する実装・提案はしない。
**設計判断（アーキテクチャの変更・スキーマの変更・新しい仕組みの追加）はこのファイルの範囲外であり、実装者が独断で行わない。提案として運営者に返すこと。**

---

## 1. アーキテクチャの不変条件（違反禁止）

1. **Sheets は審査台、JSON は公開データ。両者を直結させない。**
   - Google Sheets には未審査の申請・運営メモが含まれる。ブラウザ（公開サイト）から Sheets を直接読む実装（「ウェブに公開」CSV / gviz / API直叩き等）は、どんなに効率的でも**絶対に採用しない**。
   - 公開サイトが読んでよいデータは、リポジトリ内の静的 JSON（`data/*.json`）のみ。
2. **状態（DB・サーバーサイド処理）を持たない。**
   - 予約状況・残数・会員などの状態管理を導入しない。サイトは純粋な静的サイト（Cloudflare Pages・ビルドコマンドなし）。
   - 「便利になるから」を理由に DB / KV / Workers / サーバーレス関数を足す提案をしない。導入は運用が限界に来たと運営者が判断した時のみ。
3. **公開のトリガーは手動。**
   - Sheets→JSON の変換・公開は、運営者が承認後に手で1コマンド実行する（承認行為と公開行為の統合）。
   - 書き込み検知や cron による自動公開は実装しない（未審査データが公開に流れる事故、および承認と公開の分離を防ぐため）。
   - push 前に git diff で生成 JSON を目視確認できる形を保つ。
4. **git push が公開。** push 後は Cloudflare Pages が自動デプロイする。デプロイ設定（フレームワークなし・ビルドなし・出力ルート）は変更しない。

## 2. セキュリティ（違反禁止）

- **service account の鍵 JSON をリポジトリに絶対に入れない。** 鍵はリポジトリ外に置き、パスは環境変数か設定ファイル（gitignore 対象）で渡す。`.gitignore` に鍵ファイルのパターンを必ず入れる。
- **公開 JSON（`data/*.json`）に運営用情報を出さない。** 審査メモ・主催者の連絡先・誓約チェックの生データ・Tally の内部 ID 等は Sheets に留める。公開 JSON に載せてよいのは公開表示に必要な項目のみ。
- 公開サイトに申請者が直接書き込める経路を作らない。R2 / リポジトリへの書き込みは運営（変換スクリプト）のみ。
- 申請由来の文字列（イベント名・会場名等）は DOM へ `textContent` で挿入する。`innerHTML` への直接展開をしない（既存 `js/app.js` の方式を維持）。

## 3. リポジトリ構成

```
index.html        … サイト本体（HTML構造のみ）
css/style.css     … 全スタイル
js/app.js         … 全スクリプト（イベントカレンダー描画 renderCal を含む）
data/events.json  … 公開イベントデータ（現状サンプル。将来は変換スクリプトの出力先）
README.md         … デプロイ手順・スキーマ定義
CLAUDE.md         … 本ファイル
```

- これ以上のファイル分割（CSS複数化・JSモジュール化・ビルドツール導入・フレームワーク導入）は行わない。
- 相対パス前提（`./css/` `./js/` `./data/`）。階層構造を変えない。

## 4. events.json スキーマ（連携の契約・勝手に変えない）

`js/app.js` の描画ロジックと、将来の Sheets→JSON 変換スクリプトの両方がこのスキーマに依存する。
フィールド名・構造の変更は両者同時の変更を要するため、独断で変更しない。

```json
{
  "generated_at": "ISO 8601日時",
  "events": [
    {
      "id":    "一意なID（形式自由）",
      "name":  "イベント名",
      "date":  "YYYY-MM-DD（必須）",
      "time":  "HH:MM（任意）",
      "venue": "会場名（任意）",
      "area":  "エリア（任意）",
      "url":   "告知ページURL（任意。あれば行全体がリンク）"
    }
  ]
}
```

- フロント側仕様：過去日は非表示・日付昇順・読み込み中/0件/エラーの3状態表示あり。
- 承認フラグは Sheets 側で**申請単位**（イベント単位ではない）。変換スクリプトは承認済み申請の行を最大10イベントに展開して JSON に吐く。

## 5. データパイプライン（全体像）

```
申請者 → Tally フォーム（ログイン不要）
  → Google Sheets（種別ごとにシート＝審査台。Tally 連携で自動追記）
  → 運営が審査し、手動列「承認」を立てる
  → 変換スクリプト（手動実行）: 承認済み行のみ抽出 → data/events.json 生成 → git push
  → Cloudflare Pages 自動デプロイ → 公開
```

- Sheets の列構成の正本は **Tally フォームの質問項目**。Sheets を手で先に作らない。
- 運営用の手動列（承認・審査メモ・掲載終了日）は Tally 生成列の右側に追加し、Tally は触らない。
- **変換スクリプトは Tally フォーム作成 → Sheet 生成 → 列ヘッダ確定の後に実装する。** 列構成を想像で仮定してスクリプトを先に書かない。

## 6. ラジオプレーヤー実装の必須要件

プレーヤーは本サービスで最も故障しやすく、かつ事業の最重要仮説（ながら聴取・回遊）の生命線。
**素の `<audio>` タグだけの実装は不可。**

1. **Media Session API を実装する。** ロック画面・コントロールセンターに再生 UI とメタデータを出す。
2. **瞬断からの自動再接続を実装する。** 再生意図がある間だけ、指数バックオフ（2s→最大15s）＋キャッシュバスターで再接続。`playing` 復帰でリセット。
3. **音のエンジンは車輪の再発明をしない。** AzuraCast 公開プレーヤーの実装を土台・参考にし、自作するのは UI の殻。

### 6-1. メタ表示は2ソース（番組枠とフィラーで取得元が違う）
表示のタイトル/サムネは、性質の違う2ソースから来る。混同禁止。

- **番組枠**：Sheets 由来の編成 JSON ＋ クライアントの時刻マッチングで表示。焼き込み番組は Now Playing API では中身が1エントリに潰れて使えないため、API を表示源にしない。
- **フィラー区間**：Now Playing API（`api/nowplaying/uraten`）を **20秒ポーリング**して表示。独立トラックの BGM は API がタイトルを返す。ICY メタは使わない。
- **WebSocket / SSE は現状不採用。** ポーリングで実用上困らない（焼き込み中はタイトルが動かない）。即時反映が実際に要るシグナルが出てから差し替える（実装差し替えであって設定変更ではない）。
- **表示ガード**：`title`/`artist` が空、または `.mp3` 等 内部ファイル名っぽい値は表示しない。タイトルは「URATEN」フォールバック、アートは出さない。

### 6-2. 表示ソースの優先順位と境界の競合防止【JSON連携時に実装】
番組枠（JSON＋時刻）とフィラー（APIポーリング）は別々の時計で動くため、境界（例 20:00:00）で競合する。書き込む権利は**その瞬間のモード判定に合致したソースだけ**が持つ。

1. 「今 番組枠か / フィラー区間か」を JSON＋現在時刻で高頻度（毎秒程度）に判定する。ローカル計算のみ・負荷ゼロ。ポーリングとは切り離して回す。
2. **番組枠と判定される間は、API のレスポンスを表示に反映しない**（受信しても破棄）。番組表示は JSON が正。
3. フィラー区間の間のみ API 結果を採用する。
4. **非同期の追い越しを殺す。** fetch は投げた時と返る時でモードが変わりうる（19:59:59 発火→20:00:00 直後に返る）。**ポーリング結果を使う直前にモード判定をやり直し、番組枠なら破棄する。**「投げた時のモード」でなく「使う瞬間のモード」で採否を決める。

### 6-3. 対象外・割り切り
- **端末時計のズレ**：時刻マッチングはブラウザのローカル時計依存。境界が端末ごとに数秒ずれるが実害なし。NTP 同期等は持ち込まない。
- **短尺フィラー（5〜10秒）の取りこぼし**：20秒ポーリングでは拾えないことがあるが、表示は切り替えなくてよい。焼き込みに内包された埋めフィラーは元々表示対象外。独立トラックで表示させたい BGM は最低尺（例 30秒以上）で編成する側で対処し、ポーリング高速化/WS化はしない。

### 6-4. 実機検証の状況（2026-07 時点）
VPS・AzuraCast 稼働済み（`radio.ura-ten.jp`）のため、実機検証は着手可能。

- [x] PC バックグラウンド（別タブ/最小化）で再生継続
- [x] スマホ ロック画面で再生継続
- [x] 瞬断復帰（機内モード数秒→復帰で自動再接続）
- [ ] ロック画面の再生/停止コントロールが操作できるか（iOS Safari はアート/ボタンが出ないクセあり。要実機）

## 7. 現在の状態（2026-07 時点）

**完了：**
- Cloudflare Pages で公開中（Git 接続・自動デプロイ稼働）
- CSS / JS のファイル分割済み
- VS Code + git + Claude Code の編集ライン確立
- VPS 契約・AzuraCast 構築・稼働中（ゲート C-1 クリア）
- ドメイン `ura-ten.jp` 取得済み・自分名義（配信は `radio.ura-ten.jp`、SSL取得/自動更新済み）。公開サイトは現在 pages.dev で、ドメイン切り替え後もサイト側の変更は不要
- ラジオプレーヤー：再生/停止・バックグラウンド再生・ロック画面・瞬断自動再接続を実機確認済み（残：ロック画面コントロールの操作、iOS Safari 要確認）

**未着手・未確定：**
- Tally フォーム未作成 → Sheets 列スキーマ未確定 → 変換スクリプト未着手
- service account・鍵の取得（未実施。実施時は本ファイル2章を厳守）
- プレーヤーのメタ表示（タイトル/サムネ）：JSON連携と同時に実装（6-1・6-2）。現状はポーリングのみで暫定
- 決済は当面銀行振込（Stripe は審査通過後にフォームへ一体化。サイト側実装なし）
- イベントカレンダーは `data/events.json` 駆動（サンプル4件）

## 8. 実装時の作法

- 変更は小さく刻んでコミットする。コミットメッセージは日本語でよい。
- 見た目の変更（デザイン・文言・レイアウト）は自由に実装してよい。ただし 4章のスキーマ、2章のセキュリティ、3章の構成は影響範囲に含めない。
- 本ファイルと衝突する指示・アイデアが出た場合、実装せず「CLAUDE.md の◯章と衝突する」と運営者に確認を返す。
- 決定が変わったら本ファイルを更新する（更新も運営者の確認を経る）。

## 9. 背景（判断に迷ったときの参照）

- 事業の芯：「お金で枠を買って自分を押し出すのではなく、互いを紹介し合うことで全体が浮上する」。Pay to Win になる機能（金で露出・順位が買える仕組み）は作らない。
- 設計原則：一人で保守できる範囲に収める／静的で済むうちは静的で／必要が実証される前にインフラを作らない。
- 詳細な事業設計・権利設計・サービスメニューは Notion 管理の事業ドキュメントが正本（本リポジトリには置かない）。実装判断に必要な結論は本ファイルに抽出済み。
